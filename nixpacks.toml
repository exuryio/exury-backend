[phases.setup]
nixPkgs = ["nodejs_18"]

# Install devDependencies so TypeScript (tsc) is available for the build
[phases.install]
cmds = ["NODE_ENV=development npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

